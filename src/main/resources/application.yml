spring:
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/languagesapi?autoReconnect=true&useSSL=false
    username: julio
    password: julio-senha